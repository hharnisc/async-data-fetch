{
  "name": "@hharnisc/async-data-fetch",
  "version": "1.2.4",
  "description": "Fetch data from RPC backend",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "build": "babel src -d lib",
    "prepublish": "babel src -d lib"
  },
  "author": "hharnisc@gmail.com",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-export-extensions": "^6.22.0",
    "babel-preset-env": "^1.6.1",
    "deep-freeze": "0.0.1",
    "jest": "^22.0.4",
    "then-sleep": "^1.0.1"
  },
  "dependencies": {
    "@hharnisc/micro-rpc-client": "^0.1.2"
  },
  "babel": {
    "presets": [
      "env"
    ],
    "plugins": [
      "transform-export-extensions"
    ]
  },
  "files": [
    "lib/"
  ]
}
